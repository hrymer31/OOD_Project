public class ID {
    //UI flow #3
    //- Continue working with an order using its ID.
    //- If ID is invalid, return to prior step with warning.
    //- If ID is valid:
    // If Trip is Awaiting Travelers, return to adding Travelers.
    // If Trip is Awaiting Packages, return to adding Packages.
    // If Trip is Awaiting Payment, return to adding Payment.
    // If Trip is Awaiting Thank You Note, return to adding Thank You Note.
    // If Trip is Itinerary Ready, display the Itinerary
}
